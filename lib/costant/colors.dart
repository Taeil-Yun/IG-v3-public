import 'package:flutter/material.dart';

class ColorConfig {
  // final _theme = ig-publicMain.themeNotifier.value;

  /// primary / color code = #572dff
  Color primary({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFF572dff).withOpacity(opacity)
        : const Color(0xFF572dff).withOpacity(opacity);

    return color;
  }

  /// primary light / color code = #a893ff
  Color primaryLight({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFa893ff).withOpacity(opacity)
        : const Color(0xFFa893ff).withOpacity(opacity);

    return color;
  }

  /// primary light2 / color code = #d4c9ff
  Color primaryLight2({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFd4c9ff).withOpacity(opacity)
        : const Color(0xFFd4c9ff).withOpacity(opacity);

    return color;
  }

  /// primary light3 / color code = #f7f5ff
  Color primaryLight3({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFf7f5ff).withOpacity(opacity)
        : const Color(0xFFf7f5ff).withOpacity(opacity);

    return color;
  }

  /// accent / color code = #ff295c
  Color accent({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFff295c).withOpacity(opacity)
        : const Color(0xFFff295c).withOpacity(opacity);

    return color;
  }

  /// accent light / color code = #ff8da8
  Color accentLight({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFff8da8).withOpacity(opacity)
        : const Color(0xFFff8da8).withOpacity(opacity);

    return color;
  }

  /// dark / color code = #121016
  Color dark({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFF121016).withOpacity(opacity)
        : const Color(0xFF121016).withOpacity(opacity);

    return color;
  }

  /// white / color code = #ffffff
  Color white({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFffffff).withOpacity(opacity)
        : const Color(0xFFffffff).withOpacity(opacity);

    return color;
  }

  /// light / color code = #f7f5ff
  Color light({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFf7f5ff).withOpacity(opacity)
        : const Color(0xFFf7f5ff).withOpacity(opacity);

    return color;
  }

  /// gray1 / color code = #f7f7fa
  Color gray1({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFf7f7fa).withOpacity(opacity)
        : const Color(0xFFf7f7fa).withOpacity(opacity);

    return color;
  }

  /// gray2 / color code = #e7e7f1
  Color gray2({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFe7e7f1).withOpacity(opacity)
        : const Color(0xFFe7e7f1).withOpacity(opacity);

    return color;
  }

  /// gray3 / color code = #9393a7
  Color gray3({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFF9393a7).withOpacity(opacity)
        : const Color(0xFF9393a7).withOpacity(opacity);

    return color;
  }

  /// gray4 / color code = #686889
  Color gray4({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFF686889).withOpacity(opacity)
        : const Color(0xFF686889).withOpacity(opacity);

    return color;
  }

  /// gray5 / color code = #424260
  Color gray5({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFF424260).withOpacity(opacity)
        : const Color(0xFF424260).withOpacity(opacity);

    return color;
  }

  /// border gray1 / color code = #c6c6d9
  Color borderGray1({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFc6c6d9).withOpacity(opacity)
        : const Color(0xFFc6c6d9).withOpacity(opacity);

    return color;
  }

  /// border gray2 / color code = #d4d4db
  Color borderGray2({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFd4d4db).withOpacity(opacity)
        : const Color(0xFFd4d4db).withOpacity(opacity);

    return color;
  }

  /// border gray3 / color code = #d0d0d0
  Color borderGray3({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFd0d0d0).withOpacity(opacity)
        : const Color(0xFFd0d0d0).withOpacity(opacity);

    return color;
  }

  /// divider1 / color code = #f3f3f3
  Color divider1({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFf3f3f3).withOpacity(opacity)
        : const Color(0xFFf3f3f3).withOpacity(opacity);

    return color;
  }

  /// success / color code = #40ca67
  Color success({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFF40ca67).withOpacity(opacity)
        : const Color(0xFF40ca67).withOpacity(opacity);

    return color;
  }

  /// ui bg / color code = #121016, opacity = 80%
  Color uibg({double opacity = 0.8}) {
    Color color = 1 == 1
        ? const Color(0xFF121016).withOpacity(opacity)
        : const Color(0xFF121016).withOpacity(opacity);

    return color;
  }

  /// overlay / color code = #000000, opacity = 60%
  Color overlay({double opacity = 0.6}) {
    Color color = 1 == 1
        ? const Color(0xFF000000).withOpacity(opacity)
        : const Color(0xFF000000).withOpacity(opacity);

    return color;
  }

  /// kakao background / color code = #fee500
  Color kakaoBackground({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFFfee500).withOpacity(opacity)
        : const Color(0xFFfee500).withOpacity(opacity);

    return color;
  }

  /// naver background / color code = #03c75a
  Color naverBackground({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFF03c75a).withOpacity(opacity)
        : const Color(0xFF03c75a).withOpacity(opacity);

    return color;
  }

  /// facebook background / color code = #1877f2
  Color facebookBackground({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFF1877f2).withOpacity(opacity)
        : const Color(0xFF1877f2).withOpacity(opacity);

    return color;
  }

  /// tag label background / color code = #1f1534, opacity = 80%
  Color tagLabelBackground({double opacity = 0.8}) {
    Color color = 1 == 1
        ? const Color(0xFF1f1534).withOpacity(opacity)
        : const Color(0xFF1f1534).withOpacity(opacity);

    return color;
  }

  /// main poster background / color code = #111016
  Color mainPosterBackground({double opacity = 1}) {
    Color color = 1 == 1
        ? const Color(0xFF111016).withOpacity(opacity)
        : const Color(0xFF111016).withOpacity(opacity);

    return color;
  }

  /// won 아이콘 색 / color code = #ff9f0f
  static const wonIconColor = Color(0xFFff9f0f);

  /// point 아이콘 색 / color conde = #572dff
  static const pointIconColor = Color(0xFF572dff);

  /// info 아이콘 색 / color conde = #572dff
  static const infoIconColor = Color(0xFF2e3a59);

  /// 기본 검은색 / color code = #000000
  static const defaultBlack = Color(0xFF000000);

  /// 기본 흰색 / color code = #ffffff
  static const defaultWhite = Color(0xFFffffff);

  /// 투명 / color code = RGBA(0.0.0.0)
  static const transparent = Colors.transparent;
}
